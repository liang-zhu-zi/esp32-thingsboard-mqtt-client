TODO List
============

* Security option `testing`.
* Device provisioning `testing`.

* A new examples of all in one.

* Docs...
* Append supported ThingsBoard MQTT API in commonents/README.md.
* Append some pictures of ThingsBoard in examples/.../README.md.

* Dead lock & protected data in tb_mqtt.c/.h & tb_mqtt_helper.c/h.

* Subscribe/unsubscribe fw_chunk_response topic if sw & fw are upgrading at same time.
* OTA update support MD5-checksum-ALG.
* Software OTA upgrade.

-----------------------------------------------------------------------
* Is default login data correct?
    constexpr char PROV_ACCESS_TOKEN[] = PSTR("provision");
    constexpr char DEFAULT_CLIENT_ID[] = PSTR("TbDev");

* OTA update only has Firmware OTA and Software OTA at most, no need to form a list.
* Provision has at most one request and does not require a list.

------------------------------------------------------------------------
* Is there only one parameter for QoS of ThingsBoard MQTT API, which does not need to appear in every publish API?

------------------------------------------------------------------------
* MQTT Gateway API.
